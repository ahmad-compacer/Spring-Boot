package com.Bank.Bank.Database;

/*import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PutMapping;
import org.springframework.web.bind.annotation.RequestBody;
import com.Bank.Bank.Accounts;
import com.Bank.Bank.MainController;

public class PutMoney {
	@PutMapping("/put")
	 void putMoney(@RequestBody String newBalance, @PathVariable Integer id) {
	    
	    return MainController.accountsRepository.findById(id).map(balance > 
	    {balance.setBalance(newBalance.getBalance());
	    return MainController.accountsRepository.save(balance);)}
	      
	 }
}*/
